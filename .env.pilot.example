# Copy to .env.pilot. Keep this file outside version control and chmod 600 it on Linux.
# Use a long URL-safe password because Compose also places it in DATABASE_URL.
POSTGRES_PASSWORD=replace-with-a-long-url-safe-password
APP_PORT=3000
PILOT_VERSION=pilot-local
LOG_LEVEL=info
